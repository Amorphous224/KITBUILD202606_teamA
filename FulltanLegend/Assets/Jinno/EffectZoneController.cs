using UnityEngine;

public class EffectZoneController : MonoBehaviour
{
    // 効果カード（黄色）の移動や2回目のクリックによる消滅（地下ワープ）の処理は、
    // すべて『CardController.cs』のOnMouseDown内に完璧に実装されています。
    // そのため、このEffectZoneControllerはエラーを起こさないクリーンな状態で定義します。

    void Start()
    {
        // ゾーン生成時の初期化処理（必要に応じて追加可能）
    }

    void Update()
    {
        // 毎フレームの監視処理（必要に応じて追加可能）
    }
}
using UnityEngine;

public class EffectZoneController : MonoBehaviour
{
    // 効果カード（黄色）の移動や2回目のクリックによる消滅（地下ワープ）の処理は、すべて『CardController.cs』のOnMouseDown内に実装している
    // そのため、このEffectZoneControllerはエラーを起こさないクリーンな状態で定義する

    void Start()
    {
        // ゾーン生成時の初期化処理
    }

    void Update()
    {
        // 毎フレームの監視処理
    }
}